{% extends "base.html" %}
{%block content%}
{%set title = "Posts"%}
  <h2> {{message}} </h2></center>
  <br><br><br>
  <i><h4>Post your own content:</h4></i>
  <form action = "/post/post" method = "POST">
  
   Title: <br><input type = "text" name = "title" > <br>
   Contents:<br> <textarea name="content" style="width:250px;height:150px;"></textarea> <br>
   <br>
   Tags (Separate by Commas): <br><input type = "text" name = "tags" > <br>
   
   <br>
   <input type = "submit" value = "Post!" > 
  </form>
  <br>

<b><br> _________________<br></b>
 {% for i in range(dic|length) %}
   <div id = "post_body">
    <div id = "post_header">
     <center><b> <h3> {{dic[dic|length-i - 1]["title"]}} </h3> </b>
    </div>
    <i>by <a href = "/post/user/{{dic[dic|length-i - 1]['user']}}"> {{dic[dic|length-i - 1]["user"]}} </a> </i></center>

    <div id = "post_text">
     {{dic[dic|length-i - 1]["content"]}}
    </div>
    <b>Tags: </b><i>
    {% for j in range(tags[dic|length-i - 1]|length) %}
    <a href = "/post/tag/{{tags[dic|length-i - 1][j]}}" >{{tags[dic|length-i - 1][j]}}</a> ,
    {%endfor%}</i>
    <br>
    {% for j in dic["replies"] %}
     <i>{{j[1]}} :</i> <br> {{j[0]}} <br> ________________ <br>
    {% endfor %}

    <form action = "/post/reply/{{dic|length-i - 1}}" method = "POST">
     <textarea name = "reply" style="width:200px;height:60px;"></textarea>
     <input type = "submit" value = "Reply" >
    </form>

    </div>
    <br><br>
 {%endfor%}

{%endblock%}
